# 《落境：未知记忆》新手指南与常见问题

最后更新：2026-07-31

## 基础操作

### 如何聊天

启动游戏后，桌面会显示角色“落”。默认情况下，角色下方会显示聊天输入栏，输入内容后点击“发送”即可对话。

如果看不到输入栏：右键角色 → 应用设置 → 界面 → 开启“显示输入框”。

### 调整角色位置和大小

- 移动位置：鼠标左键按住角色身体区域拖动。
- 调整大小：鼠标放在角色身上，按住左键并滚动滚轮。
- 保存位置：右键角色 → 应用设置 → 界面 → 开启“记住角色位置”。

### 切换角色和导入 Mod

右键角色 → MOD 管理。

- 切换角色：在列表中选择角色，点击“启用”。
- 导入 Mod：将文件拖入导入区域，或点击导入区域选择文件。

支持格式：

- Live2D Runtime `.zip`
- 图片角色 `.png`、`.jpg`、`.jpeg`、`.webp`、`.gif`

Live2D 压缩包需要包含有效的 Runtime 文件，例如 `.model3.json`、`.moc3` 和贴图资源。

## 记忆点

记忆点是 AI 对话和部分云端生成功能的消耗单位。

当前赠送规则：

- 首次登录赠送 85 记忆点。
- 每月赠送 5 记忆点。
- 新用户首次登录后通常显示 90 记忆点。

当前消耗规则：

| 功能 | 消耗 |
| --- | ---: |
| 普通聊天 | 成功回复后 0.25 记忆点 |
| 文件总结 / 需要 AI 生成内容的文件请求 | 通常按普通聊天扣 0.25 记忆点 |
| 屏幕主动回复 | 气泡实际显示后 1 记忆点 |
| 屏幕摘要 | 实际使用或显示后 1 记忆点 |
| 本地窗口判断、场景分类、Mod 管理、设置调整 | 免费 |

失败、超时、服务不可用或屏幕主动回复未实际显示时，不扣记忆点。系统会保留 1 记忆点的余额保护，余额过低时可能拒绝新的扣费请求。

## 屏幕感知

屏幕感知需要用户手动开启。开启后，系统会尝试判断当前大致场景，例如工作、学习、游戏、视频、文件管理、空闲或隐私场景。

可能处理的信息：

- 前台应用名称。
- 当前窗口标题。
- 本地场景分类结果。
- 可选的本地 OCR 文字识别结果。
- 可选的本地视觉信号，例如游戏 HUD、对话框、地图等低敏特征。

隐私说明：

- 聊天、邮箱、支付、密码、隐身窗口等隐私场景会暂停识别和主动回复。
- 截图不会保存到本地文件。
- 云端主动回复不会上传屏幕截图、图片数据、完整本地路径、密码、API Key、Token 或剪贴板内容。
- 云端生成只接收低敏场景描述，例如活动类型、应用类别、停留时间或安全视觉信号摘要。

## 日志位置

正式版默认数据目录：

```text
%APPDATA%\Luo\storage
```

常见文件：

```text
%APPDATA%\Luo\storage\logs\electron-runtime.log
%APPDATA%\Luo\storage\chat-transcripts\YYYY-MM-DD.txt
%APPDATA%\Luo\storage\settings\app-settings.json
%APPDATA%\Luo\storage\file-task-logs\file-tasks.jsonl
```

反馈 Bug 时，建议附上问题发生时间、现象描述和相关日志。

## 无法启动时先做什么

1. 优先从 Steam 客户端启动游戏。
2. 打开任务管理器，结束残留的《落境：未知记忆》进程。
3. 如果看到残留的内置 `node.exe` 进程，也一并结束。
4. 重新启动 Steam，再启动游戏。
5. 仍无法启动时，查看并反馈：

```text
%APPDATA%\Luo\storage\logs\electron-runtime.log
```

不建议直接删除整个 `storage` 目录，因为其中可能包含设置、对话记录、Mod 和删除记录。需要重置前请先备份。

## 反馈入口

- Steam 商店页 / 社区讨论区
- 开发者联系邮箱：louyeshi1@zohomail.cn
