# 《落境：未知记忆》新手指南与常见问题

最后更新：2026-07-31

欢迎来到《落境：未知记忆》。本指南说明正式版中常用功能的入口、记忆点规则、屏幕感知隐私说明、日志位置，以及遇到启动问题时的初步处理方式。

## 一、新手指南

### 1. 如何打开聊天窗口

启动游戏后，桌面上会显示角色“落”。默认情况下，角色下方会显示聊天输入栏。

你可以直接在输入栏中输入内容，点击“发送”或按回车与落对话。

如果看不到输入栏：

1. 右键点击角色。
2. 打开“角色菜单”。
3. 进入“应用设置”。
4. 切换到“界面”。
5. 开启“显示输入框”。

开启后，主界面会重新显示聊天输入栏。

### 2. 如何调整角色位置和大小

角色位置：

- 用鼠标左键按住角色身体区域拖动，即可移动角色位置。
- 如果设置中开启了“记住角色位置”，下次启动时会恢复上一次的位置。

角色大小：

- 鼠标放在角色身上。
- 按住鼠标左键的同时滚动鼠标滚轮。
- 向上滚动放大，向下滚动缩小。

相关设置入口：

```text
右键角色 → 应用设置 → 界面 → 角色显示
```

这里可以设置：

- 是否允许拖拽角色。
- 是否记住角色位置。
- 回复气泡是否跟随角色。
- 是否显示聊天输入框。

### 3. 如何切换角色

1. 右键点击角色。
2. 打开“MOD 管理”。
3. 在列表中选择想使用的角色。
4. 点击“启用”。

当前启用的角色会显示为“当前”。

### 4. 如何导入 Mod

1. 右键点击角色。
2. 打开“MOD 管理”。
3. 将 Mod 文件拖入导入区域，或点击导入区域选择文件。
4. 导入成功后，在列表中点击“启用”。

当前支持的导入格式包括：

- Live2D Runtime `.zip`
- 图片角色：`.png`、`.jpg`、`.jpeg`、`.webp`、`.gif`

如果导入失败，请确认压缩包内包含有效的 Live2D Runtime 文件，例如 `.model3.json`、`.moc3` 和贴图资源。不要将 Live2D 编辑工程文件直接当作 Runtime Mod 导入。

### 5. 记忆点是什么

记忆点是《落境：未知记忆》中用于 AI 对话和部分云端生成功能的消耗单位。

玩家通过 Steam 登录后会拥有记忆点余额。普通聊天、屏幕主动回复等功能在成功使用后会按规则消耗记忆点。

本地功能不会全部扣费。例如本地窗口判断、屏幕场景分类、Mod 管理、设置调整等不会消耗记忆点。

### 6. 每种功能大致如何消耗记忆点

当前规则如下：

| 功能 | 消耗 |
| --- | ---: |
| 普通聊天 | 成功回复后扣 0.25 记忆点 |
| 文件总结 / 需要 AI 生成内容的文件请求 | 通常按普通聊天规则扣 0.25 记忆点 |
| 屏幕主动回复 | 气泡实际显示后扣 1 记忆点 |
| 屏幕摘要 | 实际使用 / 显示后扣 1 记忆点 |
| 本地屏幕识别、窗口判断、场景分类 | 免费 |
| Mod 导入、切换角色、调整位置、设置修改 | 免费 |
| 本地文件读取、删除记录、恢复等本地执行流程 | 本地执行本身免费；如需要 AI 总结或生成计划，按 AI 调用规则消耗 |

补充说明：

- 普通聊天失败、AI 服务超时或远程服务不可用时，不扣记忆点。
- 屏幕主动回复生成后，如果最终没有显示气泡，不扣记忆点。
- 系统保留 1 记忆点的余额保护，余额过低时可能会拒绝新的扣费请求。

### 7. 每月赠送规则

当前赠送规则：

- 首次登录赠送：85 记忆点。
- 每月赠送：5 记忆点。
- 新用户首次登录后通常会显示 90 记忆点。

也就是：首次赠送 85 点 + 当月赠送 5 点。

### 8. 屏幕感知会处理哪些信息

屏幕感知默认需要玩家手动开启。开启后，落会尝试判断你当前大致处于什么场景，例如：

- 工作
- 学习
- 看视频
- 游戏
- 绘画
- 文件管理
- 空闲
- 隐私场景

根据当前设置，屏幕感知可能处理的信息包括：

- 前台应用名称。
- 当前窗口标题。
- 本地场景分类结果。
- 可选的本地 OCR 识别结果。
- 可选的本地视觉信号，例如游戏 HUD、对话框、地图等低敏特征。

隐私保护规则：

- 聊天、邮箱、支付、密码、隐身窗口等隐私场景会暂停识别和主动回复。
- 截图不会保存到本地文件。
- 云端主动回复不会上传屏幕截图。
- 云端主动回复不会发送原始截图、图片数据、完整本地路径、密码、API Key、Token、剪贴板内容等敏感信息。
- 用于云端生成的内容会尽量压缩为低敏场景描述，例如“正在学习”“正在游戏”“窗口类别”等。

### 9. 日志文件在哪里

正式版默认数据目录位于：

```text
%APPDATA%\Luo\storage
```

常见日志和配置位置：

```text
%APPDATA%\Luo\storage\logs\electron-runtime.log
%APPDATA%\Luo\storage\chat-transcripts\YYYY-MM-DD.txt
%APPDATA%\Luo\storage\settings\app-settings.json
%APPDATA%\Luo\storage\file-task-logs\file-tasks.jsonl
```

其中：

- `electron-runtime.log`：启动、窗口、后端进程、Steam、崩溃相关运行日志。
- `chat-transcripts\YYYY-MM-DD.txt`：当天对话、操作与部分屏幕感知记录。
- `app-settings.json`：应用设置。
- `file-tasks.jsonl`：文件任务相关记录。

## 二、常见问题

### Q1：启动后看不到聊天输入栏怎么办？

右键角色 → 应用设置 → 界面 → 打开“显示输入框”。

如果角色也看不到，可以先退出游戏后重新启动。

### Q2：为什么我拖不动角色？

请检查：

```text
右键角色 → 应用设置 → 界面 → 允许拖拽角色
```

确认该选项已开启。

### Q3：为什么角色大小调不了？

调整大小需要：

```text
鼠标放在角色身上 → 按住鼠标左键 → 滚动滚轮
```

单独滚动滚轮不会调整大小。

### Q4：Mod 导入失败怎么办？

请先确认文件格式是否支持：

- `.zip`
- `.png`
- `.jpg`
- `.jpeg`
- `.webp`
- `.gif`

如果是 Live2D Mod，请确认压缩包里有有效的 Runtime 结构，至少需要 `.model3.json`、`.moc3` 和贴图文件。

### Q5：为什么普通聊天要消耗记忆点？

普通聊天需要调用远程 AI 服务生成回复。成功生成回复后，会扣除 0.25 记忆点。失败、超时或服务不可用时不扣费。

### Q6：为什么屏幕主动回复扣得比普通聊天多？

屏幕主动回复会根据当前场景生成主动气泡。它只有在气泡实际显示后才扣 1 记忆点；如果生成后没有显示、场景过期、窗口切换或回执失败，不会扣费。

### Q7：本地屏幕感知会不会一直扣费？

不会。本地窗口判断、场景分类、本地识别本身免费。只有需要云端生成主动回复，并且气泡实际显示后，才会扣记忆点。

### Q8：无法启动时先做什么？

建议按顺序尝试：

1. 从 Steam 客户端内启动游戏。
2. 确认没有旧版本游戏进程残留。
3. 打开任务管理器，结束残留的《落境：未知记忆》进程。
4. 如看到残留的内置 `node.exe` 进程，也一起结束。
5. 重新启动 Steam，再启动游戏。
6. 如果仍无法启动，查看日志：

```text
%APPDATA%\Luo\storage\logs\electron-runtime.log
```

反馈问题时，建议附上启动时间、现象描述、日志文件，以及是否从 Steam 启动。

### Q9：删除日志或设置能解决问题吗？

不建议一上来删除整个 `storage` 目录，因为里面可能包含设置、对话记录、Mod、删除记录等数据。

如果需要排查，优先提供日志反馈。确实要重置设置时，可以先备份：

```text
%APPDATA%\Luo\storage
```

再进行处理。

## 三、反馈入口

- Steam 商店页 / 社区讨论区
- 开发者联系邮箱：louyeshi1@zohomail.cn
