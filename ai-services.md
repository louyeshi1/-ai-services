# 外部 AI 服务

## 概述

本应用可以连接由用户自行选择并配置的第三方 AI 服务。这些服务用于生成对话回复、编程解释、代码片段、主动陪伴消息，以及受限制的文件修改计划。

开发者不运营外部 AI 模型，也不会代表用户自动选择、购买或开通任何 AI 服务。实际使用的 AI 服务取决于用户在应用内配置的 Provider、模型名称、服务地址和 API Key。

如果用户没有配置有效的外部 AI Provider 和 API Key，云端生成类 AI 功能将不可用，也不会产生外部 AI 使用费用。

## 支持的服务

本应用支持以下服务或接口格式：

- DeepSeek API  
  https://platform.deepseek.com/

- OpenAI API  
  https://platform.openai.com/

- OpenRouter  
  https://openrouter.ai/

- 用户自行配置的 OpenAI 兼容服务

- 用户自行配置的 Anthropic 兼容服务

对于兼容自定义服务，用户需要提供服务 URL、模型名称和 API Key。实际服务提供方取决于用户的配置。


## 发送到 AI 服务的信息

根据用户使用的功能不同，本应用可能会向外部 AI 服务发送：

- 用户输入的消息、问题或指令。
- 有限的近期对话上下文。
- 用户请求的文件任务所需的文本或代码内容。
- 受限制的结构化文件修改请求。
- 用户启用屏幕感知后，用于生成短回复的低敏结构化屏幕场景信息。

本应用只发送完成对应 AI 功能所需的信息。文件变更类任务中，AI 只用于生成计划；实际文件读取、校验、确认和修改由本地工具链执行。

## 屏幕感知隐私

屏幕图像在本地处理。通过屏幕感知主动回复系统调用外部 AI 时，不会发送以下信息：

- 屏幕截图。
- 图片数据。
- 原始 OCR 文本。
- 本地文件路径。
- 密码。
- API Key。
- 身份验证 Token。
- 剪贴板内容。

屏幕感知云端回复只会在用户开启相关功能后，发送低敏结构化信息，例如检测到的活动类型、应用类别、游戏场景状态、场景停留时间、清洗后的窗口名称或安全视觉信号摘要，用于生成一句简短的陪伴回复。

如果当前屏幕被识别为隐私场景，或待发送内容包含敏感字段、验证码、Token、API Key、本地路径等隐私模式，系统会阻止该次云端屏幕感知回复请求。

## 费用和计费

外部 AI 使用费用由用户选择的 Provider 根据其定价和计费条款直接收取。

本应用不销售 AI 点数、用量 Token 或 AI 订阅服务。开发者不会从第三方 API 费用中分成。

可能产生第三方费用的操作包括：

- 普通聊天回复。
- 文件总结。
- 历史对话摘要。
- 复杂文件操作计划生成。
- 屏幕感知云端短回复。
- Claude Code / Anthropic 兼容旧链路调用。
- 用户手动测试 Provider 连接。

没有有效 Provider 和 API Key 时，云端生成类 AI 功能不可用，也不会产生外部 AI 使用费用。开启屏幕感知云端短回复后，后台可能在满足条件时自动产生短请求调用，用户应在了解所选 Provider 计费规则后再开启。

## 用户控制

用户可以：

- 选择或更换 AI Provider。
- 选择模型名称和服务 URL。
- 添加、删除或替换 API Key。
- 设置默认聊天 Provider。
- 设置默认 Agent Provider。
- 关闭屏幕感知。
- 停止使用外部 AI 功能。
- 不配置任何外部 AI Provider。
- 对文件变更类操作进行确认或取消。

用户删除 Provider 后，本应用不应继续使用该 Provider 发起新的外部 AI 请求。若第三方服务已经保存历史请求，其删除范围、保留周期和处理方式以该第三方服务条款为准。

## 内容和安全控制

请求会在进入 AI 或文件处理链路前进行安全检查。本应用会拦截涉及以下内容的请求：

- 恶意软件。
- 网络攻击。
- 凭证窃取。
- 违法活动。
- 软件破解。
- 许可证、订阅或付费限制绕过。
- DRM 或反作弊绕过。
- 泄露源代码。
- 钓鱼。
- 复制非公开源代码。

文件操作相关请求还会经过本地路径授权、路径黑名单、结构化计划校验和用户确认流程。AI 生成的计划不能绕过本地确认直接修改文件。

AI 生成内容可能不准确。用户应在使用前自行检查生成的文本、代码和文件修改计划。涉及医疗、法律、财务、安全等重要事项时，应以专业人士或权威来源为准。

## 第三方条款

每个外部 AI Provider 都有自己的隐私政策、服务条款、数据保留实践、价格、地区可用性和内容规则。

用户有责任查看并接受自己所选 Provider 的政策。开发者不负责第三方 Provider 的条款变更、服务中断、计费决定或数据处理实践。

可能涉及的第三方包括但不限于：

- DeepSeek。
- OpenAI。
- OpenRouter。
- 用户自行配置的 OpenAI 兼容服务。
- 用户自行配置的 Anthropic 兼容服务。

如果用户配置自建服务或本地模型接口，应确保该接口来源可信，并理解其数据存储、日志和安全策略。

## 联系方式

如需咨询或反馈，请联系：

**开发者联系邮箱：** [louyeshi1@zohomail.cn]

**最后更新：** 2026-06-11
